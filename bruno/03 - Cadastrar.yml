info:
  name: 03 - Cadastrar
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:5001/api/drivers
  body:
    type: json
    data: |-
      {
        "titulo": "Fernando Alonso"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
