info:
  name: 06 - Confirmar remoção
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5001/api/drivers/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
